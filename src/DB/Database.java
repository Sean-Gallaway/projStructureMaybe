package DB;

import java.util.ArrayList;

public class Database {
    Card[] allCards;
    ArrayList<Card[]> playerDecks;

    // get cards with some sort of filter.
    public Card[] getCards () {
        return null;
    }

    // we can use serialization to store the class versions of the cards.
}
